<br>
<div class="booyah-box col-5">
  <h1>Add a New Lesson</h1>
  <br>
  <%= simple_form_for @lesson, url: instructor_section_lessons_path(@section) do |f| %>
    <%= f.input :title %>
    <%= f.input :subtitle %>
    <%= f.submit 'Add the lesson', class: 'btn btn-success' %>
    <br>
    <br>
  <% end %>
</div>
